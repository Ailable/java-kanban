package Models;

import Managers.Managers;
import Models.Epic;
import org.junit.jupiter.api.BeforeAll;

class EpicTest {
    /*
    @Test
    void getSubtasksListTest(){
        Models.Epic epic = Managers.Managers.getDefault().getEpicById(1);
        Managers.Managers.getDefault().getEpicSubtascs(1).add(epic);
    }

    Не вижу смысла делать тест на проверку, что объект Models.Epic нельзя добавить в самого себя в виде подзадачи,
    т.к List<Models.Subtask> subtasksList; может хранить только сабтаски и эпики запихать в него не выйдет
     */


}